import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // Mengimpor App yang baru saja di-update

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
